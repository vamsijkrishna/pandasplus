global:
    seperator: ","
    name: pums
    source : "/Users/jspeiser/pums_entire_usa/usa_<year>_<est>yr_<part>.csv"
    source_vars:
        year:
            start: 2010
            end: 2010
        est:
            start: 5
            end: 5
        part: ["a"] #, "b", "c", "d"]

    output : "/Users/jspeiser/pums_output/<year>"

    # type must be the original name of the column (before rename)
    type:
        occp02 : "str"
        occp10 : "str"
        POWSP: "str"


    POWPUMA:
        ifmissing: "XXXXX"

    agg: "sum"

    transform:
        df:
            type: frame_func
            func: "plugins.census.pums.process"


tables:
    ygi:
        pk: ["year", "geo"]

